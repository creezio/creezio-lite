/**
 * Faux curseur IA — SoT déplacée dans `@creezio/browser-host`
 * (driver-scripts.ts, partagé avec le Chromium sidecar serveur).
 * Réexport pour compat imports existants electron-shell / marques.
 */
export { FAKE_CURSOR_INJECT } from "@creezio/browser-host";

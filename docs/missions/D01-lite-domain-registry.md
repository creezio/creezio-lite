# D01 — Registre, opérations et portées du kit

Lire AGENTS.md, START-HERE.md, docs/MODULES.md, docs/ARCHITECTURE.md puis docs/contracts/domain-extension.md. Ce dernier est le contrat partagé avec l’application ; ne pas inventer une seconde API. Le métier Certivan et sa base de connaissances se trouvent dans le dépôt certivan-bo, mais ils ne doivent pas être copiés dans le code générique du kit.

Livrer du code complet et testé du registre module/entity/collection rétrocompatible, helpers AppOperationDefinition et commandes, ScopeProvider read/write avec défaut compatible, intégration au catalogue/HTTP/MCP/assistant/OpenAPI/navigation et protection des lectures/écritures/fichiers. Suivre les interfaces précises du contrat. Ne pas créer un dispatcher parallèle. Les handlers applicatifs reçoivent un contexte fiable après les contrôles existants. Tout accès direct à handleApi doit conserver les garanties. Les collections ne créent pas de navigation et déclarent un parentField réel. Augmenter la limite du registre à 64 avec validation des parents/cycles. Clients natif reste inchangé. Pas de CRUD d’écriture pour entity/collection ; toutes leurs mutations sont des commandes déclarées.

Fichiers attribués : runtime/core/{types,validation,registry,operations,commands,scope,api,search,tools}.ts ; sites-adapter/{catalog,dispatch,nav}.ts ; composants de liste/détail/navigation et primitives strictement nécessaires pour kind/parentField, tests nouveaux tests/domain-registry.test.mjs et tests/domain-operations.test.mjs, docs/reviews/D01-reception.md. Les exports existants réellement nécessaires peuvent être étendus avec justification dans la PR. Aucun fichier agent-providers, aucune table cv_*, aucun fichier observability.ts. Ne pas modifier package/lockfile, migrations appliquées, version du kit, CHANGELOG ou les docs globales des autres missions.

Le correctif B00 de PR14 modifie actuellement observability.ts et les traces dans sites-adapter/src/dispatch.ts. Travailler sur sa branche dédiée ; avant livraison, récupérer main et conserver ce correctif si fusionné. Toute collision dans dispatch doit être résolue sans rétablir les corps de requêtes. Ne pas modifier la branche B00.

Tests indispensables : ancien module sans kind identique ; entity/collection CRUD refusé en HTTP et appel direct ; GET history et POST commande visibles au catalogue/OpenAPI/MCP avec schémas de chemin/query/corps validés ; commande nommée get reste POST ; collisions routes/IDs/outils refusées ; droits live session/token/OAuth/assistant ; read grant insuffisant pour write ; filtres avant liste/count/search/snippets ; fichiers download et delete ; provider deleteFile traite toute suppression sans fallback R2 concurrent ; owner/admin/member/viewer et espaces croisés ; module absent/parentField absent/cycle/65 modules invalides.

Exécuter contrôles AGENTS dans l’ordre (npm test puis check/template typecheck/build/exemples). Utiliser des fixtures fictives et le runtime workerd/Miniflare du kit pour les garanties D1 si nécessaire. Pas de services externes payants, secrets, autre agent, merge ou déploiement. Ouvrir/pousser une PR et rendre les résultats exacts ainsi que les imports à raccorder dans Certivan. Le but est le code de fondation exécutable, pas une nouvelle proposition documentaire.

# No application secret is required for Sites identity or managed D1/R2.
# Add keys only for an explicitly implemented external integration.
# Configure production values in the Site settings, never in hosting.json.
